# This repo ships workflow instructions only. It must never contain company or
# personal data. The workspace lives outside the repo by default; these rules are
# belt-and-braces in case someone points a workspace at the repo root.

workspace/
*/workspace/

# Workspace artifacts, wherever they land
TASKS.md
CLAUDE.md
PROFILE.md
memory/
briefs/
reviews/
ledger/
decisions/
log/
board.html

# ...except the templates and the shipped board, which are the whole point
!chief-of-staff/templates/
!chief-of-staff/templates/**
!chief-of-staff/skills/assets/board.html

.DS_Store
.env
.env.*
node_modules/
